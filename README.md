# bronco
Airflow running Terraform in Docker


### To run a DAG

clone the repo, then:

~~~
docker-compose build
docker-compose up -d
~~~

Navigate to http://127.0.0.1:8080/

username: `airflow`

password: `airflow`